import SwiftUI

/// Optional menu view for the game
struct MenuView: View {
    var body: some View {
        VStack {
            Text("Pizza Chicken Pop")
                .font(.largeTitle)
            
            // TODO: Add menu buttons and UI
        }
    }
}
